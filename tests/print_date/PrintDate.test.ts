describe('PrintDate', () => {
  test('printDate', () => {
    // const printDate = new PrintDate(here calendar strict mock, here printer spy);

    // printDate.printCurrentDate();

    // How can we test this function?
  });
});
